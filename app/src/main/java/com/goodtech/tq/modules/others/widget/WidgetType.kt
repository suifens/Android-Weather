package com.goodtech.tq.modules.others.widget

/**
 * 桌面小部件样式类型。
 *
 * - [SingleLine1] / [SingleLine2]：单行小尺寸 widget 的两种样式（非透明 / 透明）
 * - [DoubleLine1] / [DoubleLine2]：双行大尺寸 widget 的两种样式（非透明 / 透明）
 */
enum class WidgetType {
    SingleLine1,
    SingleLine2,
    DoubleLine1,
    DoubleLine2
}
